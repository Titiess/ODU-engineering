export const lessons: string[] = [
  "Building multi-wallet connection flows requires careful handling of provider-specific edge cases like popup blockers and auto-disconnect timeouts.",
  "Zustand's computed selectors pattern is highly effective for filtering large proposal datasets without unnecessary re-renders.",
  "Designing a responsive sidebar navigation that gracefully degrades from full labels to icon-only mode requires CSS grid with named areas rather than simple flex toggling.",
  "Dark theme design systems benefit from defining opacity-based color tokens rather than hardcoded hex values for consistent transparency layering."
];
